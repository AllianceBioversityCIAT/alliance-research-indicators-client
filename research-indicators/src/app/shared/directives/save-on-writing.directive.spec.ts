import { SaveOnWritingDirective } from './save-on-writing.directive';

describe('SaveOnWritingDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveOnWritingDirective();
    expect(directive).toBeTruthy();
  });
});
